{
  "name": "paytm-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "Backend",
    "PayTm"
  ],
  "scripts": {
    "start": "cd Backend && npm start",
    "dev": "cd Backend && npm run dev",
    "build": "cd PayTm && npm install && npm run build"
  }
}
